<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<title>MIZUYA — Modern Asia, After Dark | Çorum</title>
<meta name="description" content="MIZUYA, Çorum'un ilk modern Asya mutfağı deneyimi. Japon ve Kore mutfağından ilham alan sushi, ramen ve daha fazlası. Fen Lisesi Cd. No:1, Çepni / Çorum.">
<link rel="canonical" href="https://mizuya.tr/">

<meta property="og:type" content="restaurant">
<meta property="og:title" content="MIZUYA — Modern Asia, After Dark">
<meta property="og:description" content="Çorum'un ilk Asia mutfağı. Japonya, Kore ve Asya mutfağından ilham alan çağdaş bir gastronomi deneyimi.">
<meta property="og:url" content="https://mizuya.tr/">
<meta property="og:locale" content="tr_TR">
<meta property="og:image" content="https://images.unsplash.com/photo-1611143669185-af224c5e3252?auto=format&fit=crop&w=1200&q=80">

<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="MIZUYA — Modern Asia, After Dark">
<meta name="twitter:description" content="Çorum'un ilk Asia mutfağı deneyimi.">
<meta name="twitter:image" content="https://images.unsplash.com/photo-1611143669185-af224c5e3252?auto=format&fit=crop&w=1200&q=80">

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="style.css">

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Restaurant",
  "name": "MIZUYA",
  "servesCuisine": ["Japanese", "Korean", "Asian Fusion", "Sushi", "Ramen"],
  "priceRange": "₺₺₺",
  "telephone": "+905300101797",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Fen Lisesi Cd. No:1 Cd. 23D, Çepni",
    "addressLocality": "Çorum",
    "addressCountry": "TR"
  },
  "openingHoursSpecification": {
    "@type": "OpeningHoursSpecification",
    "dayOfWeek": ["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"],
    "opens": "12:00",
    "closes": "23:00"
  },
  "sameAs": ["https://www.instagram.com/mizuya.tr"]
}
</script>
</head>
<body>

<!-- LOADER -->
<div id="loader" aria-hidden="true">
  <div class="loader-mark">MIZUYA</div>
  <div class="loader-tag">01 — EXPERIENCE</div>
  <div class="loader-bar"></div>
</div>

<!-- CUSTOM CURSOR -->
<div class="cursor-dot" id="cursorDot"></div>
<div class="cursor-label" id="cursorLabel"></div>

<!-- NAV -->
<header class="site-nav" id="siteNav">
  <a href="#top" class="nav-mark" data-cursor="open">MIZUYA</a>
  <nav class="nav-right">
    <a href="#reservation" class="nav-link">Rezervasyon</a>
    <a href="#contact" class="nav-link">İletişim</a>
    <button class="nav-link nav-menu-trigger" id="menuOpenBtn" data-cursor="menu">Menü</button>
    <a href="#reservation" class="nav-reserve" data-cursor="open">Rezervasyon Yap</a>
  </nav>
</header>

<!-- SCROLL PROGRESS -->
<div class="progress-rail" id="progressRail">
  <span data-idx="0">01</span>
  <span data-idx="1">02</span>
  <span data-idx="2">03</span>
  <span data-idx="3">04</span>
  <span data-idx="4">05</span>
  <span data-idx="5">06</span>
  <span data-idx="6">07</span>
  <span data-idx="7">08</span>
  <span data-idx="8">09</span>
</div>

<main id="top">

  <!-- 01 — HERO -->
  <section class="scene hero" data-progress-mark data-cursor="explore">
    <div class="scene-media">
      <video autoplay muted loop playsinline preload="metadata" poster="https://images.unsplash.com/photo-1611143669185-af224c5e3252?auto=format&fit=crop&w=1600&q=60">
        <source src="assets/mizuya-sushi-yapimi.mp4" type="video/mp4">
      </video>
    </div>
    <div class="scene-scrim"></div>
    <div class="grain"></div>
    <div class="scene-content" style="display:flex;flex-direction:column;min-height:calc(100svh - 9.5rem);">
      <div class="hero-top">
        <span class="hero-mark">MIZUYA</span>
        <span class="hero-loc">Çorum<br>Türkiye</span>
      </div>
      <div style="margin-top:auto;">
        <h1 class="hero-headline" data-reveal>ASIA,<br><em>AFTER DARK.</em></h1>
        <p class="hero-sub" data-reveal data-reveal-delay="1">Japonya, Kore ve Asya mutfağından ilham alan çağdaş bir gastronomi deneyimi.</p>
        <div class="hero-bottom">
          <div class="scroll-cue"><span class="line"></span> Aşağı Kaydır</div>
        </div>
      </div>
    </div>
  </section>

  <!-- 02 — STORY -->
  <section class="scene story" data-progress-mark>
    <span class="story-num">02 / HİKÂYE</span>
    <h2 class="story-head word-reveal" data-split>Çorum'un ilk Asia mutfağı.</h2>
    <div class="story-foot">
      <p data-reveal>Mizuya, mahalle mutfağının sıcaklığıyla uzak doğunun sabrını aynı sofraya taşıyor.</p>
      <p data-reveal data-reveal-delay="1">Her tabak; pirinç, ateş ve bekleme üzerine kurulu küçük bir ritüel.</p>
    </div>
  </section>

  <!-- 03 — RITUAL -->
  <section class="scene ritual" data-progress-mark>
    <div class="ritual-grid">
      <div>
        <span class="eyebrow" data-reveal>03 / Sushi Nedir</span>
        <h2 class="ritual-head word-reveal" data-split style="margin-top:.6rem;">Sushi bir ritüeldir.</h2>
        <ul class="ritual-list">
          <li data-reveal>Pirincin dengesi.</li>
          <li data-reveal data-reveal-delay="1">Malzemenin tazeliği.</li>
          <li data-reveal data-reveal-delay="2">Kesimin hassasiyeti.</li>
          <li data-reveal data-reveal-delay="3">El işçiliği.</li>
        </ul>
      </div>
      <div class="ritual-media" data-reveal-media>
        <img src="https://images.unsplash.com/photo-1611143669185-af224c5e3252?auto=format&fit=crop&w=1200&q=80" alt="Elde hazırlanan sushi, yakın plan" loading="lazy">
      </div>
    </div>
  </section>

  <!-- 04/05 — CRAFT VIDEO SCENE -->
  <div class="craft-wrap" id="craftWrap" data-progress-mark>
    <div class="craft-sticky">
      <div class="craft-frame" id="craftFrame">
        <video muted loop playsinline preload="metadata" id="craftVideo"
          poster="https://images.unsplash.com/photo-1611143669185-af224c5e3252?auto=format&fit=crop&w=1200&q=60">
          <source src="assets/mizuya-sushi-yapimi.mp4" type="video/mp4">
        </video>
      </div>
      <div class="craft-caption" id="craftCaption">
        <span class="eyebrow">05 / CRAFT</span>
        <h3>Bir sushi, ellerde başlar.</h3>
      </div>
      <div class="craft-hint" id="craftHint">Kaydırmaya devam edin</div>
    </div>
  </div>

  <!-- 06 — JAPAN × KOREA -->
  <section class="duel" id="duel" data-progress-mark>
    <div class="duel-side duel-side--left" id="duelJapan">
      <img src="https://images.unsplash.com/photo-1574060275764-4e3e046ff29c?auto=format&fit=crop&w=1400&q=75" alt="Kyoto, gece fenerleri" loading="lazy">
      <div class="duel-label"><span class="eyebrow">Tokyo</span><h3>JAPAN</h3></div>
    </div>
    <div class="duel-side duel-side--right" id="duelKorea">
      <img src="https://images.unsplash.com/photo-1585951641542-c30dac5d87b0?auto=format&fit=crop&w=1400&q=75" alt="Kore usulü ızgara" loading="lazy">
      <div class="duel-label"><span class="eyebrow">Seoul</span><h3>KOREA</h3></div>
    </div>
    <div class="duel-center"><span>MIZUYA</span><small>05 / İKİ MUTFAK</small></div>
  </section>

  <!-- 07 — INGREDIENTS -->
  <section class="scene ingredients" data-progress-mark>
    <div class="ing-head">
      <span class="eyebrow" data-reveal>06 / Detaylar</span>
      <h2 data-reveal data-reveal-delay="1">Details make the difference.</h2>
    </div>
    <div class="ing-grid">
      <div class="ing-item a"><img src="https://images.unsplash.com/photo-1591814468924-caf88d1232e1?auto=format&fit=crop&w=1000&q=75" alt="Pirinç" loading="lazy"><span class="ing-tag">Pirinç</span></div>
      <div class="ing-item b"><img src="https://images.unsplash.com/photo-1579584425555-c3ce17fd4351?auto=format&fit=crop&w=1000&q=75" alt="Somon" loading="lazy"><span class="ing-tag">Somon</span></div>
      <div class="ing-item c"><img src="https://images.unsplash.com/photo-1553621042-f6e147245754?auto=format&fit=crop&w=800&q=75" alt="Nori" loading="lazy"><span class="ing-tag">Nori</span></div>
      <div class="ing-item d"><img src="https://images.unsplash.com/photo-1633478062482-790e3b5dd810?auto=format&fit=crop&w=800&q=75" alt="Zencefil" loading="lazy"><span class="ing-tag">Zencefil</span></div>
      <div class="ing-item e"><img src="https://images.unsplash.com/photo-1617196034796-73dfa7b1fd56?auto=format&fit=crop&w=800&q=75" alt="Susam" loading="lazy"><span class="ing-tag">Susam</span></div>
      <div class="ing-item f"><img src="https://images.unsplash.com/photo-1607301405418-780ee5e6dd10?auto=format&fit=crop&w=1400&q=75" alt="Soya sosu" loading="lazy"><span class="ing-tag">Soya</span></div>
    </div>
  </section>

  <!-- 08 — HORIZONTAL PROCESS -->
  <div class="process-wrap" id="processWrap" data-progress-mark>
    <div class="process-sticky">
      <div class="process-progress" id="processProgress">
        <span class="is-active"></span><span></span><span></span><span></span><span></span>
      </div>
      <div class="process-track" id="processTrack">
        <div class="process-panel">
          <img src="https://images.unsplash.com/photo-1591814468924-caf88d1232e1?auto=format&fit=crop&w=1600&q=75" alt="Pirinç hazırlığı" loading="lazy">
          <div class="process-info"><span class="eyebrow">01 / RICE</span><h3>Pirincin sabrı.</h3></div>
        </div>
        <div class="process-panel">
          <img src="https://images.unsplash.com/photo-1730900737644-e146f78db8e7?auto=format&fit=crop&w=1600&q=75" alt="El işçiliği" loading="lazy">
          <div class="process-info"><span class="eyebrow">02 / CRAFT</span><h3>Elin hassasiyeti.</h3></div>
        </div>
        <div class="process-panel">
          <img src="https://images.unsplash.com/photo-1585951641542-c30dac5d87b0?auto=format&fit=crop&w=1600&q=75" alt="Ateş" loading="lazy">
          <div class="process-info"><span class="eyebrow">03 / FLAME</span><h3>Ateşin sıcaklığı.</h3></div>
        </div>
        <div class="process-panel">
          <img src="https://images.unsplash.com/photo-1607301405418-780ee5e6dd10?auto=format&fit=crop&w=1600&q=75" alt="Sos" loading="lazy">
          <div class="process-info"><span class="eyebrow">04 / SAUCE</span><h3>Dengenin sırrı.</h3></div>
        </div>
        <div class="process-panel">
          <img src="https://images.unsplash.com/photo-1611143669185-af224c5e3252?auto=format&fit=crop&w=1600&q=75" alt="Tabak" loading="lazy">
          <div class="process-info"><span class="eyebrow">05 / PLATE</span><h3>Son dokunuş.</h3></div>
        </div>
      </div>
    </div>
  </div>

  <!-- 09 — AFTER DARK -->
  <section class="scene afterdark" id="afterDark" data-progress-mark>
    <div class="scene-media"><img src="https://images.unsplash.com/photo-1736673084495-be9439760067?auto=format&fit=crop&w=1800&q=70" alt="Gece, Tokyo sokağı" loading="lazy"></div>
    <div class="scene-scrim"></div>
    <div class="afterdark-glow" id="afterDarkGlow"></div>
    <div class="scene-content">
      <span class="eyebrow" data-reveal>08 / After Dark</span>
      <h2 data-reveal data-reveal-delay="1">Gece<br>başlar.</h2>
    </div>
  </section>

  <!-- GALLERY -->
  <section class="gallery" data-progress-mark>
    <div class="gallery-head">
      <span class="eyebrow" data-reveal>09 / Görüntüler</span>
      <h2 data-reveal data-reveal-delay="1">Sofradan kareler.</h2>
    </div>
    <div class="gallery-grid">
      <div class="g-item g1"><img src="https://images.unsplash.com/photo-1633478062482-790e3b5dd810?auto=format&fit=crop&w=1400&q=75" alt="Sushi tabağı" loading="lazy"></div>
      <div class="g-item g2"><img src="https://images.unsplash.com/photo-1569718212165-3a8278d5f624?auto=format&fit=crop&w=900&q=75" alt="Ramen" loading="lazy"></div>
      <div class="g-item g3"><img src="https://images.unsplash.com/photo-1548150914-c9f19106dbf6?auto=format&fit=crop&w=900&q=75" alt="Kore ızgara" loading="lazy"></div>
      <div class="g-item g4"><img src="https://images.unsplash.com/photo-1675870791718-a12568cfef43?auto=format&fit=crop&w=1000&q=75" alt="Sushi detay" loading="lazy"></div>
      <div class="g-item g5"><img src="https://images.unsplash.com/photo-1574060275764-4e3e046ff29c?auto=format&fit=crop&w=900&q=75" alt="Fenerler" loading="lazy"></div>
      <div class="g-item g6"><img src="https://images.unsplash.com/photo-1591325418441-ff678baf78ef?auto=format&fit=crop&w=1600&q=75" alt="Ramen kase" loading="lazy"></div>
      <div class="g-item g7"><img src="https://images.unsplash.com/photo-1553621042-f6e147245754?auto=format&fit=crop&w=900&q=75" alt="Sushi çeşitleri" loading="lazy"></div>
    </div>
  </section>

  <!-- RESERVATION -->
  <section class="scene reserve" id="reservation">
    <span class="eyebrow" data-reveal>Rezervasyon</span>
    <h2 data-reveal data-reveal-delay="1">Masada<br>görüşelim.</h2>
    <a href="tel:+905300101797" class="reserve-btn" data-reveal data-reveal-delay="2" data-cursor="open">Rezervasyon Yap</a>
    <p class="reserve-phone" data-reveal data-reveal-delay="3"><a href="tel:+905300101797">+90 530 010 17 97</a></p>
  </section>

  <!-- CONTACT -->
  <section class="contact" id="contact">
    <div class="contact-grid">
      <div class="contact-block">
        <span class="eyebrow">Adres</span>
        <p>Fen Lisesi Cd. No:1 Cd. 23D, Çepni / Çorum</p>
      </div>
      <div class="contact-block">
        <span class="eyebrow">Saatler</span>
        <p>12:00 — 23:00</p>
      </div>
      <div class="contact-block">
        <span class="eyebrow">Instagram</span>
        <p><a href="https://instagram.com/mizuya.tr" target="_blank" rel="noopener">@mizuya.tr</a></p>
      </div>
    </div>
  </section>
</main>

<!-- MARQUEE -->
<div class="marquee" aria-hidden="true">
  <div class="marquee-track">
    <span>MIZUYA</span><span>MODERN ASIA</span><span>MIZUYA</span><span>MODERN ASIA</span>
    <span>MIZUYA</span><span>MODERN ASIA</span><span>MIZUYA</span><span>MODERN ASIA</span>
  </div>
</div>

<!-- FOOTER -->
<footer class="site-footer">
  <div class="footer-mark">MIZUYA</div>
  <div class="footer-row">
    <span>Modern Asia / After Dark</span>
    <div class="footer-links">
      <a href="#reservation">Rezervasyon</a>
      <a href="#contact">İletişim</a>
      <a href="https://instagram.com/mizuya.tr" target="_blank" rel="noopener">Instagram</a>
    </div>
    <span>© 2026 Mizuya, Çorum</span>
  </div>
</footer>

<!-- FULLSCREEN MENU -->
<div class="menu-overlay" id="menuOverlay" role="dialog" aria-modal="true" aria-label="Menü">
  <div class="menu-overlay-top">
    <span class="nav-mark">MIZUYA</span>
    <button class="menu-close" id="menuCloseBtn"><span class="x">×</span> Kapat</button>
  </div>
  <div class="menu-body">
    <div class="menu-cats" id="menuCats">
      <button class="menu-cat is-active" data-cat="sushi">Sushi</button>
      <button class="menu-cat" data-cat="starters">Starters</button>
      <button class="menu-cat" data-cat="ramen">Ramen</button>
      <button class="menu-cat" data-cat="asian">Asian</button>
      <button class="menu-cat" data-cat="korean">Korean</button>
      <button class="menu-cat" data-cat="drinks">Drinks</button>
      <button class="menu-cat" data-cat="dessert">Dessert</button>
    </div>

    <ul class="menu-items is-active" data-cat-items="sushi">
      <li class="menu-item"><span class="menu-item-name">Mizuya Roll<span class="menu-item-note">Somon, avokado, kremalı peynir</span></span><span class="menu-item-price">₺320</span></li>
      <li class="menu-item"><span class="menu-item-name">Dragon Roll<span class="menu-item-note">Karides tempura, avokado, unagi sos</span></span><span class="menu-item-price">₺340</span></li>
      <li class="menu-item"><span class="menu-item-name">Sake Nigiri (4 adet)<span class="menu-item-note">Somon</span></span><span class="menu-item-price">₺260</span></li>
      <li class="menu-item"><span class="menu-item-name">Spicy Tuna Roll<span class="menu-item-note">Ton balığı, acı mayonez, taze soğan</span></span><span class="menu-item-price">₺310</span></li>
      <li class="menu-item"><span class="menu-item-name">Sashimi Seçkisi (9 adet)<span class="menu-item-note">Şefin günlük seçimi</span></span><span class="menu-item-price">₺420</span></li>
    </ul>

    <ul class="menu-items" data-cat-items="starters">
      <li class="menu-item"><span class="menu-item-name">Edamame<span class="menu-item-note">Deniz tuzu ile</span></span><span class="menu-item-price">₺140</span></li>
      <li class="menu-item"><span class="menu-item-name">Gyoza (6 adet)<span class="menu-item-note">Izgara mantı, ponzu sos</span></span><span class="menu-item-price">₺190</span></li>
      <li class="menu-item"><span class="menu-item-name">Tempura Karides<span class="menu-item-note">4 adet, wasabi mayonez</span></span><span class="menu-item-price">₺230</span></li>
      <li class="menu-item"><span class="menu-item-name">Tuna Tartar<span class="menu-item-note">Avokado, sriracha, kruşan</span></span><span class="menu-item-price">₺260</span></li>
    </ul>

    <ul class="menu-items" data-cat-items="ramen">
      <li class="menu-item"><span class="menu-item-name">Tonkotsu Ramen<span class="menu-item-note">Domuz suyu, chashu, yumurta</span></span><span class="menu-item-price">₺280</span></li>
      <li class="menu-item"><span class="menu-item-name">Miso Ramen<span class="menu-item-note">Tavuk suyu, mısır, tereyağı</span></span><span class="menu-item-price">₺260</span></li>
      <li class="menu-item"><span class="menu-item-name">Spicy Ramen<span class="menu-item-note">Acı biber yağı, chashu</span></span><span class="menu-item-price">₺280</span></li>
    </ul>

    <ul class="menu-items" data-cat-items="asian">
      <li class="menu-item"><span class="menu-item-name">Pad Thai<span class="menu-item-note">Karides, fıstık, taze limon</span></span><span class="menu-item-price">₺270</span></li>
      <li class="menu-item"><span class="menu-item-name">Wok Sebze<span class="menu-item-note">Mevsim sebzeleri, soya</span></span><span class="menu-item-price">₺210</span></li>
      <li class="menu-item"><span class="menu-item-name">Teriyaki Tavuk<span class="menu-item-note">Jasmin pirinç ile</span></span><span class="menu-item-price">₺250</span></li>
    </ul>

    <ul class="menu-items" data-cat-items="korean">
      <li class="menu-item"><span class="menu-item-name">Bulgogi<span class="menu-item-note">Marine dana, pirinç, kimchi</span></span><span class="menu-item-price">₺320</span></li>
      <li class="menu-item"><span class="menu-item-name">Bibimbap<span class="menu-item-note">Sebzeler, yumurta, gochujang</span></span><span class="menu-item-price">₺260</span></li>
      <li class="menu-item"><span class="menu-item-name">Kimchi Jjigae<span class="menu-item-note">Acılı kimchi güveç</span></span><span class="menu-item-price">₺240</span></li>
    </ul>

    <ul class="menu-items" data-cat-items="drinks">
      <li class="menu-item"><span class="menu-item-name">Yuzu Soda</span><span class="menu-item-price">₺120</span></li>
      <li class="menu-item"><span class="menu-item-name">Matcha Latte</span><span class="menu-item-price">₺140</span></li>
      <li class="menu-item"><span class="menu-item-name">Sakura Mocktail</span><span class="menu-item-price">₺160</span></li>
    </ul>

    <ul class="menu-items" data-cat-items="dessert">
      <li class="menu-item"><span class="menu-item-name">Matcha Tiramisu</span><span class="menu-item-price">₺160</span></li>
      <li class="menu-item"><span class="menu-item-name">Mochi (3 adet)</span><span class="menu-item-price">₺130</span></li>
      <li class="menu-item"><span class="menu-item-name">Kara Susamlı Cheesecake</span><span class="menu-item-price">₺170</span></li>
    </ul>

    <div class="menu-visual">
      <img class="is-active" data-cat-img="sushi" src="https://images.unsplash.com/photo-1611143669185-af224c5e3252?auto=format&fit=crop&w=900&q=75" alt="Sushi">
      <img data-cat-img="starters" src="https://images.unsplash.com/photo-1612927601601-6638404737ce?auto=format&fit=crop&w=900&q=75" alt="Starters">
      <img data-cat-img="ramen" src="https://images.unsplash.com/photo-1569718212165-3a8278d5f624?auto=format&fit=crop&w=900&q=75" alt="Ramen">
      <img data-cat-img="asian" src="https://images.unsplash.com/photo-1607301405418-780ee5e6dd10?auto=format&fit=crop&w=900&q=75" alt="Asian">
      <img data-cat-img="korean" src="https://images.unsplash.com/photo-1585951641542-c30dac5d87b0?auto=format&fit=crop&w=900&q=75" alt="Korean">
      <img data-cat-img="drinks" src="https://images.unsplash.com/photo-1591814468924-caf88d1232e1?auto=format&fit=crop&w=900&q=75" alt="Drinks">
      <img data-cat-img="dessert" src="https://images.unsplash.com/photo-1553621042-f6e147245754?auto=format&fit=crop&w=900&q=75" alt="Dessert">
    </div>
  </div>
</div>

<script src="script.js"></script>
</body>
</html>
/* ==========================================================================
   MIZUYA — Digital Experience
   Design tokens
   ========================================================================== */
:root{
  --ink:#090807;
  --ink-2:#14100d;
  --ink-3:#1c1712;
  --ivory:#eee7dc;
  --ivory-dim:rgba(238,231,220,.58);
  --ivory-faint:rgba(238,231,220,.14);
  --red:#8e211d;
  --red-bright:#b3372f;
  --stone:#a69f95;

  --serif:'Cormorant Garamond', serif;
  --sans:'DM Sans', sans-serif;

  --fs-hero: clamp(3.4rem, 11vw, 9.5rem);
  --fs-xl:   clamp(2.6rem, 7.2vw, 6rem);
  --fs-lg:   clamp(1.9rem, 4.6vw, 3.4rem);
  --fs-md:   clamp(1.3rem, 2.4vw, 1.7rem);
  --fs-body: clamp(1rem, 1.15vw, 1.15rem);
  --fs-sm:   .85rem;
  --fs-label:.72rem;

  --ease: cubic-bezier(.22,1,.36,1);
  --ease-soft: cubic-bezier(.4,0,.2,1);
  --dur-page: 900ms;

  --pad-edge: clamp(1.5rem, 5vw, 4.5rem);
}

/* ==========================================================================
   Reset
   ========================================================================== */
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
html{scroll-behavior:smooth;}
html,body{background:var(--ink);color:var(--ivory);}
body{
  font-family:var(--sans);
  font-size:var(--fs-body);
  line-height:1.5;
  overflow-x:hidden;
  -webkit-font-smoothing:antialiased;
  text-rendering:optimizeLegibility;
}
body.menu-open, body.no-scroll{overflow:hidden;height:100%;}
body:not(.is-loaded){overflow:hidden;height:100vh;}
img{display:block;max-width:100%;}
a{color:inherit;text-decoration:none;}
button{font:inherit;color:inherit;background:none;border:0;cursor:pointer;}
ul{list-style:none;}
h1,h2,h3{font-family:var(--serif);font-weight:500;line-height:1.02;letter-spacing:-.01em;}
::selection{background:var(--red);color:var(--ivory);}

.serif{font-family:var(--serif);}
.eyebrow{
  font-size:var(--fs-label);
  letter-spacing:.18em;
  text-transform:uppercase;
  color:var(--stone);
  font-weight:500;
}
.sr-only{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;}

@media (prefers-reduced-motion: reduce){
  *{animation-duration:.001ms !important;animation-iteration-count:1 !important;transition-duration:.001ms !important;scroll-behavior:auto !important;}
}

/* ==========================================================================
   Loader
   ========================================================================== */
#loader{
  position:fixed;inset:0;z-index:900;
  background:var(--ink);
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:1.1rem;
  transition:opacity .8s var(--ease), visibility .8s var(--ease);
}
#loader .loader-mark{
  font-family:var(--serif);
  font-size:clamp(2.2rem,6vw,3.6rem);
  letter-spacing:.06em;
  opacity:0;
  animation:loaderIn .9s var(--ease) .15s forwards;
}
#loader .loader-tag{
  font-size:var(--fs-label);
  letter-spacing:.24em;
  text-transform:uppercase;
  color:var(--stone);
  opacity:0;
  animation:loaderIn .9s var(--ease) .5s forwards;
}
#loader .loader-bar{
  width:min(220px,40vw);height:1px;background:var(--ivory-faint);
  position:relative;overflow:hidden;margin-top:.4rem;
  opacity:0;animation:loaderIn .6s var(--ease) .7s forwards;
}
#loader .loader-bar::after{
  content:'';position:absolute;inset:0;background:var(--ivory);
  transform:translateX(-100%);
  animation:loaderFill 1.4s var(--ease-soft) .8s forwards;
}
@keyframes loaderIn{to{opacity:1;}}
@keyframes loaderFill{to{transform:translateX(0);}}
body.is-loaded #loader{opacity:0;visibility:hidden;pointer-events:none;}

/* ==========================================================================
   Custom cursor (desktop only)
   ========================================================================== */
.cursor-dot{
  position:fixed;top:0;left:0;z-index:850;
  width:8px;height:8px;border-radius:50%;
  background:var(--ivory);
  transform:translate(-50%,-50%);
  pointer-events:none;
  transition:width .25s var(--ease), height .25s var(--ease), background .25s var(--ease);
  display:none;
}
.cursor-label{
  position:fixed;top:0;left:0;z-index:850;
  transform:translate(-50%,-50%);
  pointer-events:none;
  font-size:var(--fs-label);
  letter-spacing:.16em;
  text-transform:uppercase;
  color:var(--ink);
  background:var(--ivory);
  border-radius:999px;
  padding:.5em 1em;
  opacity:0;
  white-space:nowrap;
  transition:opacity .25s var(--ease);
  display:none;
}
html.has-fine-pointer:not(.reduced-motion) .cursor-dot,
html.has-fine-pointer:not(.reduced-motion) .cursor-label{display:block;}
html.has-fine-pointer:not(.reduced-motion) body{cursor:none;}
html.has-fine-pointer:not(.reduced-motion) a,
html.has-fine-pointer:not(.reduced-motion) button{cursor:none;}
.cursor-label.is-active{opacity:1;}
.cursor-dot.is-hidden{opacity:0;}

/* ==========================================================================
   Nav
   ========================================================================== */
.site-nav{
  position:fixed;top:0;left:0;right:0;z-index:400;
  display:flex;align-items:center;justify-content:space-between;
  padding:1.6rem var(--pad-edge);
  mix-blend-mode:normal;
  transition:transform .5s var(--ease), background .4s var(--ease), padding .4s var(--ease);
}
.site-nav.nav-compact{
  padding-top:1rem;padding-bottom:1rem;
  background:rgba(9,8,7,.78);
  backdrop-filter:blur(10px);
}
.nav-mark{font-family:var(--serif);font-size:1.5rem;letter-spacing:.04em;}
.nav-right{display:flex;align-items:center;gap:clamp(1rem,3vw,2.6rem);}
.nav-link{
  font-size:var(--fs-label);letter-spacing:.16em;text-transform:uppercase;
  position:relative;padding-bottom:2px;
}
.nav-link::after{
  content:'';position:absolute;left:0;bottom:0;width:100%;height:1px;
  background:var(--ivory);transform:scaleX(0);transform-origin:right;
  transition:transform .4s var(--ease);
}
.nav-link:hover::after{transform:scaleX(1);transform-origin:left;}
.nav-reserve{
  border:1px solid var(--ivory-faint);
  padding:.65em 1.4em;
  font-size:var(--fs-label);letter-spacing:.16em;text-transform:uppercase;
  transition:background .35s var(--ease), color .35s var(--ease), border-color .35s var(--ease);
}
.nav-reserve:hover{background:var(--ivory);color:var(--ink);border-color:var(--ivory);}

/* ==========================================================================
   Scroll progress rail
   ========================================================================== */
.progress-rail{
  position:fixed;right:clamp(1rem,2.4vw,2.2rem);top:50%;transform:translateY(-50%);
  z-index:300;display:flex;flex-direction:column;gap:.55rem;align-items:center;
}
.progress-rail span{
  font-size:.62rem;letter-spacing:.1em;color:var(--ivory-faint);
  transition:color .35s var(--ease), transform .35s var(--ease);
}
.progress-rail span.is-active{color:var(--ivory);transform:scale(1.15);}
@media (max-width:860px){.progress-rail{display:none;}}

/* ==========================================================================
   Scene / section shared
   ========================================================================== */
.scene{
  position:relative;min-height:100vh;
  display:flex;flex-direction:column;justify-content:center;
  padding:7rem var(--pad-edge);
  overflow:hidden;
}
.scene-media{position:absolute;inset:0;z-index:0;}
.scene-media img,.scene-media video{width:100%;height:100%;object-fit:cover;}
.scene-scrim{position:absolute;inset:0;z-index:1;}
.scene > .scene-content{position:relative;z-index:2;}

[data-reveal]{opacity:0;transform:translateY(28px);transition:opacity 1s var(--ease), transform 1s var(--ease);}
[data-reveal].is-in{opacity:1;transform:translateY(0);}
[data-reveal="fade"]{transform:none;}
[data-reveal-delay="1"]{transition-delay:.08s;}
[data-reveal-delay="2"]{transition-delay:.16s;}
[data-reveal-delay="3"]{transition-delay:.24s;}

.word-reveal .word{
  display:inline-block;
  opacity:.14;
  transform:translateY(.18em);
  transition:opacity .7s var(--ease), transform .7s var(--ease);
  transition-delay:calc(var(--i) * 45ms);
}
.word-reveal.is-in .word{opacity:1;transform:translateY(0);}

/* ==========================================================================
   HERO
   ========================================================================== */
.hero{
  min-height:100svh;padding-top:6rem;padding-bottom:3.5rem;
  justify-content:flex-end;
}
.hero .scene-media video{
  filter:saturate(.82) contrast(1.08) brightness(.62);
  transform:scale(1.02);
  animation:heroZoom 22s ease-in-out infinite alternate;
}
@keyframes heroZoom{from{transform:scale(1.02);}to{transform:scale(1.12);}}
.hero .scene-scrim{
  background:
    linear-gradient(180deg, rgba(9,8,7,.55) 0%, rgba(9,8,7,.25) 32%, rgba(9,8,7,.55) 68%, var(--ink) 100%),
    radial-gradient(ellipse at 50% 100%, rgba(9,8,7,.9), transparent 60%);
}
.hero .grain{position:absolute;inset:0;z-index:1;opacity:.18;mix-blend-mode:overlay;pointer-events:none;
  background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='120' height='120'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/></filter><rect width='100%25' height='100%25' filter='url(%23n)'/></svg>");
  background-size:180px;
}
.hero-top{
  position:relative;z-index:2;
  display:flex;justify-content:space-between;align-items:flex-start;
  margin-bottom:auto;padding-top:1rem;
}
.hero-mark{font-family:var(--serif);font-size:clamp(1.4rem,2.4vw,1.8rem);letter-spacing:.05em;}
.hero-loc{font-size:var(--fs-label);letter-spacing:.2em;text-transform:uppercase;color:var(--stone);text-align:right;}
.hero-headline{
  font-size:var(--fs-hero);
  letter-spacing:-.015em;
  margin-bottom:1.4rem;
}
.hero-headline em{font-style:italic;color:var(--ivory);}
.hero-sub{
  max-width:34ch;font-size:var(--fs-md);color:var(--ivory-dim);font-family:var(--serif);font-weight:400;
  margin-bottom:2.4rem;
}
.hero-bottom{display:flex;justify-content:space-between;align-items:flex-end;gap:1.5rem;flex-wrap:wrap;}
.scroll-cue{display:flex;align-items:center;gap:.7rem;font-size:var(--fs-label);letter-spacing:.16em;text-transform:uppercase;color:var(--stone);}
.scroll-cue .line{width:1px;height:34px;background:var(--ivory-faint);position:relative;overflow:hidden;}
.scroll-cue .line::after{content:'';position:absolute;top:-100%;left:0;width:100%;height:100%;background:var(--ivory);animation:cueDrop 2.2s var(--ease-soft) infinite;}
@keyframes cueDrop{0%{top:-100%;}60%{top:100%;}100%{top:100%;}}

/* ==========================================================================
   STORY (word reveal)
   ========================================================================== */
.story{background:var(--ink);align-items:flex-start;}
.story-head{max-width:16ch;font-size:var(--fs-hero);}
.story-foot{
  margin-top:2.2rem;display:flex;gap:clamp(2rem,8vw,7rem);flex-wrap:wrap;
  max-width:900px;
}
.story-foot p{max-width:32ch;color:var(--ivory-dim);font-size:var(--fs-body);}
.story-num{position:absolute;top:7rem;right:var(--pad-edge);font-size:var(--fs-label);letter-spacing:.2em;color:var(--stone);}

/* ==========================================================================
   RITUAL (sushi philosophy)
   ========================================================================== */
.ritual{background:var(--ink-2);}
.ritual-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:clamp(2rem,6vw,5rem);align-items:center;}
.ritual-head{font-size:var(--fs-xl);}
.ritual-list{margin-top:2rem;display:flex;flex-direction:column;gap:.9rem;}
.ritual-list li{
  font-family:var(--serif);font-size:var(--fs-lg);color:var(--ivory-dim);
  border-bottom:1px solid var(--ivory-faint);padding-bottom:.7rem;
}
.ritual-media{
  position:relative;aspect-ratio:4/5;overflow:hidden;
  clip-path:inset(6% 6% 6% 6% round 2px);
  transition:clip-path 1.2s var(--ease);
}
.ritual-media.is-in{clip-path:inset(0% 0% 0% 0% round 2px);}
.ritual-media img{width:100%;height:100%;object-fit:cover;transform:scale(1.15);transition:transform 1.6s var(--ease);}
.ritual-media.is-in img{transform:scale(1);}
@media (max-width:860px){.ritual-grid{grid-template-columns:1fr;}.ritual-media{order:-1;aspect-ratio:4/3;}}

/* ==========================================================================
   CRAFT — video grow scene
   ========================================================================== */
.craft-wrap{position:relative;height:340vh;}
.craft-sticky{
  position:sticky;top:0;height:100svh;
  display:flex;align-items:center;justify-content:center;
  overflow:hidden;background:var(--ink);
}
.craft-frame{
  position:relative;overflow:hidden;
  width:var(--craft-w,38vw);
  height:var(--craft-h,52vh);
  border-radius:var(--craft-r,4px);
  transition:border-radius .1s linear;
  box-shadow:0 40px 120px rgba(0,0,0,.55);
}
.craft-frame video{
  position:absolute;top:50%;left:50%;
  width:120%;height:120%;
  object-fit:cover;
  transform:translate(-50%,-50%) scale(var(--craft-scale,1));
  transform-origin:center;
}
.craft-caption{
  position:absolute;left:var(--pad-edge);bottom:8vh;z-index:3;
  opacity:var(--craft-caption-o,0);
  transition:opacity .5s var(--ease);
}
.craft-caption .eyebrow{margin-bottom:.6rem;display:block;}
.craft-caption h3{font-size:var(--fs-xl);max-width:10ch;}
.craft-hint{position:absolute;right:var(--pad-edge);bottom:8vh;z-index:3;font-size:var(--fs-label);
  letter-spacing:.16em;text-transform:uppercase;color:var(--stone);
  opacity:var(--craft-caption-o,0);transition:opacity .5s var(--ease);}

/* ==========================================================================
   JAPAN × KOREA
   ========================================================================== */
.duel{position:relative;min-height:100svh;display:flex;padding:0;overflow:hidden;}
.duel-side{
  position:relative;flex-basis:50%;min-width:0;
  transition:flex-basis .45s var(--ease-soft);
  display:flex;align-items:flex-end;
  overflow:hidden;
}
.duel-side img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;filter:saturate(.85) brightness(.7);}
.duel-side::after{content:'';position:absolute;inset:0;background:linear-gradient(0deg, rgba(9,8,7,.85), rgba(9,8,7,.15) 55%);}
.duel-label{position:relative;z-index:2;padding:0 0 4rem clamp(1.2rem,4vw,3rem);}
.duel-label .eyebrow{color:var(--ivory-dim);}
.duel-label h3{font-size:clamp(2.4rem,7vw,5.5rem);}
.duel-side--left .duel-label{padding-left:var(--pad-edge);}
.duel-side--right{align-items:flex-end;justify-content:flex-end;}
.duel-side--right .duel-label{text-align:right;padding-right:var(--pad-edge);padding-left:0;}
.duel-center{
  position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);z-index:3;
  text-align:center;pointer-events:none;
}
.duel-center span{display:block;font-family:var(--serif);font-size:clamp(1.3rem,2.6vw,1.9rem);letter-spacing:.04em;}
.duel-center small{display:block;font-size:var(--fs-label);letter-spacing:.2em;color:var(--stone);margin-top:.4rem;}
@media (max-width:760px){
  .duel{flex-direction:column;}
  .duel-side{flex-basis:50% !important;width:100%;}
}

/* ==========================================================================
   INGREDIENTS (editorial)
   ========================================================================== */
.ingredients{background:var(--ink);}
.ing-head{max-width:20ch;margin-bottom:clamp(2.5rem,6vw,5rem);}
.ing-head h2{font-size:var(--fs-xl);}
.ing-grid{
  display:grid;
  grid-template-columns:repeat(6,1fr);
  grid-auto-rows:clamp(150px,20vw,240px);
  gap:clamp(.8rem,2vw,1.6rem);
}
.ing-item{position:relative;overflow:hidden;min-height:100%;}
.ing-item img{width:100%;height:100%;object-fit:cover;transition:transform .8s var(--ease);}
.ing-item:hover img{transform:scale(1.06);}
.ing-item .ing-tag{
  position:absolute;left:.9rem;bottom:.8rem;z-index:2;
  font-size:var(--fs-label);letter-spacing:.16em;text-transform:uppercase;color:var(--ivory);
  text-shadow:0 2px 12px rgba(0,0,0,.6);
}
.ing-item.a{grid-column:1/4;grid-row:1/3;}
.ing-item.b{grid-column:4/7;grid-row:1/2;}
.ing-item.c{grid-column:4/6;grid-row:2/3;}
.ing-item.d{grid-column:6/7;grid-row:2/3;}
.ing-item.e{grid-column:1/3;grid-row:3/4;}
.ing-item.f{grid-column:3/7;grid-row:3/4;}
@media (max-width:860px){
  .ing-grid{grid-template-columns:repeat(2,1fr);grid-auto-rows:34vh;}
  .ing-item.a,.ing-item.b,.ing-item.c,.ing-item.d,.ing-item.e,.ing-item.f{grid-column:auto;grid-row:auto;}
}

/* ==========================================================================
   HORIZONTAL SCROLL (Süreç)
   ========================================================================== */
.process-wrap{position:relative;height:500vh;}
.process-sticky{position:sticky;top:0;height:100svh;overflow:hidden;background:var(--ink-2);}
.process-track{
  display:flex;height:100%;width:max-content;
  transform:translateX(0);
  will-change:transform;
}
.process-panel{
  position:relative;width:100vw;height:100%;flex-shrink:0;
  display:flex;align-items:flex-end;padding:5rem var(--pad-edge);
  overflow:hidden;
}
.process-panel img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;filter:brightness(.65) saturate(.9);}
.process-panel::after{content:'';position:absolute;inset:0;background:linear-gradient(0deg,rgba(9,8,7,.8),transparent 55%);}
.process-info{position:relative;z-index:2;}
.process-info .eyebrow{margin-bottom:.8rem;display:block;}
.process-info h3{font-size:clamp(2.6rem,7vw,5.5rem);}
.process-progress{position:absolute;left:var(--pad-edge);top:2.4rem;z-index:2;display:flex;gap:.5rem;}
.process-progress span{width:26px;height:1px;background:var(--ivory-faint);}
.process-progress span.is-active{background:var(--ivory);}
@media (max-width:760px){
  .process-wrap{height:auto;}
  .process-sticky{position:relative;height:auto;}
  .process-track{display:block;width:100%;transform:none !important;}
  .process-panel{width:100%;height:86vh;}
}

/* ==========================================================================
   AFTER DARK
   ========================================================================== */
.afterdark{background:var(--ink);}
.afterdark .scene-scrim{background:linear-gradient(180deg, rgba(9,8,7,.35), var(--ink) 85%);}
.afterdark .scene-media img{filter:brightness(.5) saturate(.8);}
.afterdark-glow{
  position:absolute;inset:0;z-index:1;pointer-events:none;
  background:radial-gradient(480px circle at var(--gx,50%) var(--gy,50%), rgba(142,33,29,.28), transparent 60%);
  transition:background .2s ease-out;
}
.afterdark h2{font-size:var(--fs-hero);max-width:12ch;}

/* ==========================================================================
   GALLERY
   ========================================================================== */
.gallery{padding:7rem var(--pad-edge);background:var(--ink);}
.gallery-head{margin-bottom:clamp(2.5rem,6vw,5rem);}
.gallery-head h2{font-size:var(--fs-xl);max-width:16ch;}
.gallery-grid{
  display:grid;grid-template-columns:repeat(12,1fr);
  gap:clamp(1rem,2.2vw,1.8rem);
}
.g-item{position:relative;overflow:hidden;}
.g-item img{width:100%;height:100%;object-fit:cover;}
.g-item.g1{grid-column:1/8;aspect-ratio:16/10;}
.g-item.g2{grid-column:8/13;aspect-ratio:3/4;align-self:end;}
.g-item.g3{grid-column:1/5;aspect-ratio:1/1;margin-top:2rem;}
.g-item.g4{grid-column:5/9;aspect-ratio:4/5;}
.g-item.g5{grid-column:9/13;aspect-ratio:1/1;margin-top:-3rem;}
.g-item.g6{grid-column:2/9;aspect-ratio:16/8;}
.g-item.g7{grid-column:9/13;aspect-ratio:3/4;}
@media (max-width:860px){
  .gallery-grid{grid-template-columns:1fr 1fr;}
  .g-item{grid-column:span 1 !important;margin-top:0 !important;align-self:auto !important;aspect-ratio:4/5 !important;}
}

/* ==========================================================================
   RESERVATION
   ========================================================================== */
.reserve{align-items:center;text-align:center;background:var(--ink-2);}
.reserve h2{font-size:var(--fs-hero);}
.reserve-btn{
(() => {
  'use strict';

  const html = document.documentElement;
  const body = document.body;
  const reducedMotionQuery = window.matchMedia('(prefers-reduced-motion: reduce)');
  const finePointerQuery = window.matchMedia('(pointer: fine)');
  const mobileQuery = window.matchMedia('(max-width: 860px)');

  function applyMotionPref() {
    if (reducedMotionQuery.matches) body.classList.add('reduced-motion');
    else body.classList.remove('reduced-motion');
  }
  function applyPointerPref() {
    if (finePointerQuery.matches) html.classList.add('has-fine-pointer');
    else html.classList.remove('has-fine-pointer');
  }
  applyMotionPref();
  applyPointerPref();
  reducedMotionQuery.addEventListener('change', applyMotionPref);
  finePointerQuery.addEventListener('change', applyPointerPref);

  /* ------------------------------------------------------------------ */
  /* Word splitting for headline reveals                                 */
  /* ------------------------------------------------------------------ */
  document.querySelectorAll('[data-split]').forEach((el) => {
    const text = el.textContent.trim();
    const words = text.split(/\s+/);
    el.innerHTML = words.map((w, i) => `<span class="word" style="--i:${i}">${w}</span>`).join(' ');
  });

  /* ------------------------------------------------------------------ */
  /* Loader                                                              */
  /* ------------------------------------------------------------------ */
  const minLoad = new Promise((res) => setTimeout(res, 1400));
  const pageLoad = new Promise((res) => {
    if (document.readyState === 'complete') res();
    else window.addEventListener('load', res, { once: true });
  });
  Promise.all([minLoad, pageLoad]).then(() => {
    body.classList.add('is-loaded');
    const hero = document.querySelector('.hero');
    if (hero) {
      hero.querySelectorAll('[data-reveal]').forEach((el) => el.classList.add('is-in'));
    }
  });

  /* ------------------------------------------------------------------ */
  /* Custom cursor                                                       */
  /* ------------------------------------------------------------------ */
  const cursorDot = document.getElementById('cursorDot');
  const cursorLabel = document.getElementById('cursorLabel');
  let cx = window.innerWidth / 2, cy = window.innerHeight / 2;
  let lx = cx, ly = cy;

  window.addEventListener('mousemove', (e) => {
    cx = e.clientX; cy = e.clientY;
  }, { passive: true });

  function cursorLoop() {
    lx += (cx - lx) * 0.18;
    ly += (cy - ly) * 0.18;
    if (cursorDot) cursorDot.style.transform = `translate(${cx}px, ${cy}px) translate(-50%,-50%)`;
    if (cursorLabel) cursorLabel.style.transform = `translate(${lx}px, ${ly}px) translate(-50%,-50%)`;
    requestAnimationFrame(cursorLoop);
  }
  requestAnimationFrame(cursorLoop);

  const cursorMap = { explore: 'EXPLORE', open: 'OPEN', menu: 'MENU' };
  document.querySelectorAll('[data-cursor]').forEach((el) => {
    el.addEventListener('mouseenter', () => {
      const key = el.getAttribute('data-cursor');
      if (cursorLabel && cursorMap[key]) {
        cursorLabel.textContent = cursorMap[key];
        cursorLabel.classList.add('is-active');
      }
      if (cursorDot) cursorDot.classList.add('is-hidden');
    });
    el.addEventListener('mouseleave', () => {
      if (cursorLabel) cursorLabel.classList.remove('is-active');
      if (cursorDot) cursorDot.classList.remove('is-hidden');
    });
  });

  /* ------------------------------------------------------------------ */
  /* Nav compact on scroll                                               */
  /* ------------------------------------------------------------------ */
  const siteNav = document.getElementById('siteNav');
  function onNavScroll() {
    if (window.scrollY > 40) siteNav.classList.add('nav-compact');
    else siteNav.classList.remove('nav-compact');
  }
  window.addEventListener('scroll', onNavScroll, { passive: true });
  onNavScroll();

  /* ------------------------------------------------------------------ */
  /* Generic reveal on view                                              */
  /* ------------------------------------------------------------------ */
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-in');
        revealObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.2 });

  document.querySelectorAll('[data-reveal], .word-reveal, [data-reveal-media]').forEach((el) => {
    revealObserver.observe(el);
  });
  document.querySelectorAll('.ritual-media[data-reveal-media]').forEach((el) => {
    revealObserver.observe(el);
    el.classList.add('ritual-media');
  });

  /* ------------------------------------------------------------------ */
  /* Scroll progress rail (01-09 across main scenes)                     */
  /* Computed directly from scroll position (robust against instant      */
  /* jumps and very tall sticky sections) rather than IO enter/exit.     */
  /* ------------------------------------------------------------------ */
  const marks = Array.from(document.querySelectorAll('[data-progress-mark]'));
  const railSpans = Array.from(document.querySelectorAll('#progressRail span'));
  function updateRail() {
    if (!marks.length || !railSpans.length) return;
    const probeY = window.innerHeight * 0.4;
    let activeIdx = 0;
    for (let i = 0; i < marks.length; i++) {
      if (marks[i].getBoundingClientRect().top <= probeY) activeIdx = i;
    }
    activeIdx = Math.min(activeIdx, railSpans.length - 1);
    railSpans.forEach((s, i) => s.classList.toggle('is-active', i === activeIdx));
  }

  /* ------------------------------------------------------------------ */
  /* CRAFT — video grow-to-fullscreen on scroll                          */
  /* ------------------------------------------------------------------ */
  const craftWrap = document.getElementById('craftWrap');
  const craftFrame = document.getElementById('craftFrame');
  const craftVideo = document.getElementById('craftVideo');
  const craftCaption = document.getElementById('craftCaption');
  const craftHint = document.getElementById('craftHint');

  let craftPlaying = false;
  function clamp01(v) { return Math.max(0, Math.min(1, v)); }

  function updateCraft() {
    if (!craftWrap) return;
    const rect = craftWrap.getBoundingClientRect();
    const total = rect.height - window.innerHeight;
    const scrolled = clamp01(-rect.top / (total || 1));

    // grow phase: 0 -> 0.55 goes from small to fullscreen; 0.55 -> 0.75 holds fullscreen; 0.75 -> 1 shrinks back
    let growT;
    let holding = false;
    if (scrolled < 0.5) {
      growT = clamp01(scrolled / 0.5);
    } else if (scrolled < 0.75) {
      growT = 1;
      holding = true;
    } else {
      growT = clamp01(1 - (scrolled - 0.75) / 0.25);
    }
    const ease = growT * growT * (3 - 2 * growT); // smoothstep

    const wStart = 34, wEnd = 100;
    const hStart = 46, hEnd = 100;
    const rStart = 6, rEnd = 0;

    craftFrame.style.setProperty('--craft-w', `${wStart + (wEnd - wStart) * ease}vw`);
    craftFrame.style.setProperty('--craft-h', `${hStart + (hEnd - hStart) * ease}svh`);
    craftFrame.style.setProperty('--craft-r', `${rStart + (rEnd - rStart) * ease}px`);
    craftFrame.style.setProperty('--craft-scale', `${1 + 0.06 * ease}`);
    craftCaption.style.setProperty('--craft-caption-o', holding ? 1 : 0);
    craftHint.style.setProperty('--craft-caption-o', (!holding && growT < 0.6) ? 1 : 0);

    const inView = rect.bottom > 0 && rect.top < window.innerHeight;
    if (inView && !craftPlaying) {
      craftVideo.play().catch(() => {});
      craftPlaying = true;
    } else if (!inView && craftPlaying) {
      craftVideo.pause();
      craftPlaying = false;
    }
  }

  /* ------------------------------------------------------------------ */
  /* HORIZONTAL PROCESS scroll (desktop only)                            */
  /* ------------------------------------------------------------------ */
  const processWrap = document.getElementById('processWrap');
  const processTrack = document.getElementById('processTrack');
  const processDots = processWrap ? Array.from(processWrap.querySelectorAll('.process-progress span')) : [];
  const processPanelCount = processWrap ? processWrap.querySelectorAll('.process-panel').length : 0;

  function updateProcess() {
    if (!processWrap || !processTrack || mobileQuery.matches) return;
    const rect = processWrap.getBoundingClientRect();
    const total = rect.height - window.innerHeight;
    const t = clamp01(-rect.top / (total || 1));
    const maxTranslate = (processPanelCount - 1) * window.innerWidth;
    processTrack.style.transform = `translateX(${-t * maxTranslate}px)`;
    const activeIdx = Math.min(processPanelCount - 1, Math.floor(t * processPanelCount));
    processDots.forEach((d, i) => d.classList.toggle('is-active', i === activeIdx));
  }

  /* ------------------------------------------------------------------ */
  /* AFTER DARK — mouse glow                                             */
  /* ------------------------------------------------------------------ */
  const afterDark = document.getElementById('afterDark');
  const afterDarkGlow = document.getElementById('afterDarkGlow');
  if (afterDark && afterDarkGlow) {
    afterDark.addEventListener('mousemove', (e) => {
      const rect = afterDark.getBoundingClientRect();
      const gx = ((e.clientX - rect.left) / rect.width) * 100;
      const gy = ((e.clientY - rect.top) / rect.height) * 100;
      afterDarkGlow.style.setProperty('--gx', `${gx}%`);
      afterDarkGlow.style.setProperty('--gy', `${gy}%`);
    });
  }

  /* ------------------------------------------------------------------ */
  /* JAPAN x KOREA interactive split                                     */
  /* ------------------------------------------------------------------ */
  const duel = document.getElementById('duel');
  const duelJapan = document.getElementById('duelJapan');
  const duelKorea = document.getElementById('duelKorea');
  if (duel && duelJapan && duelKorea && finePointerQuery.matches) {
    duel.addEventListener('mousemove', (e) => {
      const rect = duel.getBoundingClientRect();
      const frac = clamp01((e.clientX - rect.left) / rect.width);
      const leftPct = 35 + frac * 30; // 35 - 65
      duelJapan.style.flexBasis = `${leftPct}%`;
      duelKorea.style.flexBasis = `${100 - leftPct}%`;
    });
    duel.addEventListener('mouseleave', () => {
      duelJapan.style.flexBasis = '50%';
      duelKorea.style.flexBasis = '50%';
    });
  }

  /* ------------------------------------------------------------------ */
  /* rAF-driven scroll updates                                           */
  /* ------------------------------------------------------------------ */
  let ticking = false;
  function onScroll() {
    updateRail(); // cheap (9 rects) — always run immediately, never gated
    if (!ticking) {
      requestAnimationFrame(() => {
        updateCraft();
        updateProcess();
        ticking = false;
      });
      ticking = true;
    }
  }
  window.addEventListener('scroll', onScroll, { passive: true });
  window.addEventListener('resize', onScroll, { passive: true });
  onScroll();

  /* ------------------------------------------------------------------ */
  /* Fullscreen menu overlay                                             */
  /* ------------------------------------------------------------------ */
  const menuOverlay = document.getElementById('menuOverlay');
  const menuOpenBtn = document.getElementById('menuOpenBtn');
  const menuCloseBtn = document.getElementById('menuCloseBtn');
  const menuCats = document.querySelectorAll('.menu-cat');
  const menuItemLists = document.querySelectorAll('.menu-items');
  const menuVisualImgs = document.querySelectorAll('.menu-visual img');

  function openMenu() {
    menuOverlay.classList.add('is-open');
    body.classList.add('menu-open');
  }
  function closeMenu() {
    menuOverlay.classList.remove('is-open');
    body.classList.remove('menu-open');
  }
  if (menuOpenBtn) menuOpenBtn.addEventListener('click', openMenu);
  if (menuCloseBtn) menuCloseBtn.addEventListener('click', closeMenu);
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && menuOverlay.classList.contains('is-open')) closeMenu();
  });

  function setCategory(cat) {
    menuCats.forEach((btn) => btn.classList.toggle('is-active', btn.dataset.cat === cat));
    menuItemLists.forEach((ul) => ul.classList.toggle('is-active', ul.dataset.catItems === cat));
    menuVisualImgs.forEach((img) => img.classList.toggle('is-active', img.dataset.catImg === cat));
  }
  menuCats.forEach((btn) => {
    btn.addEventListener('mouseenter', () => setCategory(btn.dataset.cat));
    btn.addEventListener('click', () => setCategory(btn.dataset.cat));
    btn.addEventListener('focus', () => setCategory(btn.dataset.cat));
  });

})();
